version: '3'
services:

  # Next js.
  nextjs:
    container_name: nextjs
    build:
      context: ./docker-compose/nextjs
    ports:
      - 3000:3000
    restart: unless-stopped
    volumes:
      - ./:/var/www